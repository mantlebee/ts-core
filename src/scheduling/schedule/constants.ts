export enum ScheduleTypes {
  OneTime,
  Daily, // every 1 day(s)
  Weekly, // on monday and friday
  MonthlyByDays, // the 4th of the month, every 1 month(s)
  MonthlyByWeekDays, // the first monday of the month, every 1 month(s)
}
